# Clock launcher identity v1: bounded engineering amendment

Approved by the user's `ok` on 2026-09-12 in response to the section101
launcher-guard correction question. Recorded before source changes. The user's
subsequent instruction delegates routine in-scope work without repeated approval
questions, except for security/privacy concerns. Scientific changes still need
an explicit prospective record; this amendment makes none.

## Defect and exact scope

The Windows CPython3.12.9 virtual-environment redirector waits for a child whose
command replaces the first executable token with the base interpreter while
preserving the argument suffix. Section101 records the observed direct pair
(parent47880, child47732). Whole-command equality falsely rejects that launcher
as another research worker. The former positive mock did not model this rewrite.

Correct only the clock runner's process census/launcher identity check and its
regression fixtures. Update the independent auditor's necessary runner/test
fingerprints and bind this amendment in both fingerprint inventories. Historical
runner implementations, release/proof files and completed artifacts are untouched.

The exemption must identify only the current worker and its directly verified
repository-venv launcher, not arbitrary Python ancestors. Require unique valid
process records, exact PID/PPID linkage, expected parent/child executable paths
and matching executable command tokens. Compare the complete argument tails
using Windows command-line semantics; reject mismatches and malformed commands.
Keep competitor detection and fail-closed census failures. No command hiding,
runtime spoofing, process termination or guard bypass is permitted.

## Preserved research contract

V3_CLOCK_OBSERVATION_V1.md remains byte-identical at SHA256
3266b72e0687e309816375a63377e7f4a7d3fe9d9c4e8461840b8fde7153973c.
Preserve every setting, formula, source observation, reward, seed/reset stream,
PPO parameter, initial tensor, budget, stage order, name, output, metric, all16
historical gates and all5 clock-benefit conditions. No new initialization
measurement, checkpoint load, training, evaluation, replay or result audit is
part of this repair. No result, prior failure or policy designation changes.

Before any later experiment, complete the separately recorded release/proof and
fresh per-stage full-test/process/output/predecessor checks. This amendment is
not a research release and cannot satisfy or bypass those checks.

## Recoverable pre-amendment source evidence

Baseline Git commit: c65c3df7949b45f6e76b389409c28f80a3f51cb4.
The affected tracked bytes must be verified against recoverable Git objects
before modification, not merely assumed recoverable from their hashes:

- scripts/run_clock_comparison_v1.py:
  b93965c70559379449bc0232712bd3d973bfac8ef582cbdd3382e262cb7bcfa8.
- tests/test_clock_comparison_v1.py:
  566ee4bc5517add1a73e1594db8b3ce1961be1bd8693ffefde00acf6baa8402b.
- scripts/audit_clock_comparison_v1.py:
  23a7cfff76513e5225f6a880fe948b8c0569aa1bb42cafce11e06d6c73e06e3d.
- tests/test_clock_comparison_audit_v1.py (unchanged unless pin-test needs require it):
  8f07d13ba340af7e30757671879545232c88efbca81895de9ed5a1df700c5f78.

Retain all531 historical input pins,227 dependency pins,235 snapshot members
and the measured clock preparation unchanged. Preserve staged README/MILESTONES
and append every implementation, failed check, review and result to the working
MILESTONES tail. No model/log/data commit or Git push.

## Required engineering verification

Regression tests cover the real Windows full-path rewrite, quoted paths with
spaces, complete argument preservation, changed/extra/missing arguments, wrong
executables/tokens/origins, malformed or uninspectable records, duplicate/missing
identities, incorrect ancestry and additional research workers. Use fabricated
fixtures and an actual Windows non-driving launch/census; no RL episode is needed.
Independent source/test review, fresh full Ruff and pytest, final retained-input
verification and a reviewed path-scoped local commit complete this repair.
